const searchDurations = {
  enter: 0.3,
  exit: 0.75,
};

const disabledPages = [
];

const footerOverrides = {
  vincekim: {
    big: true,
    index: 0,
    role: "Website Lead",
    linkedin: "https://www.linkedin.com/in/vince-kim-profile/",
    website: "https://google.com", // dummy link, if anyone is to find it, change it (even linkedin is too much info)
    github: "https://github.com/vincekim14/",
  },
};

export { searchDurations, disabledPages, footerOverrides };
